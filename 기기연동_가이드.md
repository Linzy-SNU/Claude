# 맥북 ↔ 휴대폰 · 아이패드 실시간 연동 가이드

맥북의 Claude 작업 공간을 휴대폰과 아이패드에서 실시간으로 보고 이어서 작업하는 방법입니다.
연동은 두 가지 통로로 이루어집니다.

```
작업 공간 /Users/linzy/Claude/  ──(세션 시작 pull · 종료 커밋·푸시)──▶  GitHub  ──▶  모든 기기 (Claude 앱, GitHub 앱)
맥북 04.Claude_Output/          ──(iCloud 자동 동기화)──────────────▶  iCloud  ──▶  휴대폰/아이패드 (파일 앱)
```

## 1. 설치할 것 없음 (자동)

`scripts/git-auto-sync.sh` 훅이 `.claude/settings.json`에 등록되어 저장소에 함께 들어 있습니다.
저장소를 쓰는 **모든 기기·클라우드 세션에 자동으로 적용**되므로 따로 설치할 것이 없습니다.

- 세션을 **시작**하면 → `git pull` (최신 상태로 맞춤)
- 작업이 **끝나면** → 자동 커밋 + 푸시 (백그라운드, 기다릴 필요 없음)

안전장치로, 아래 상황에서는 자동 커밋을 건너뜁니다.

- detached HEAD(워크트리)이거나 merge·rebase가 진행 중일 때
- `origin` 원격이나 업스트림 브랜치가 없을 때
- `.env`, `*.pem`, `*.key` 등 **자격증명처럼 보이는 파일**이 섞여 있을 때 (실수로 비밀정보가 올라가는 것 방지)

동기화 로그: `~/Library/Logs/claude-git-hook.log`

## 2. 새 세션은 항상 클라우드 방식으로

Claude Code 세션을 **클라우드(웹) 방식**으로 열면 작업이 GitHub 저장소 위에서 바로 이루어지므로,
맥북·아이패드·휴대폰 어디서든 같은 세션을 실시간으로 이어서 쓸 수 있습니다.

- **맥북**: claude.ai/code 또는 Claude 앱에서 `linzy-SNU/Claude` 저장소를 선택해 새 세션 시작
- **아이패드/휴대폰**: Claude 앱을 열면 진행 중인 클라우드 세션이 그대로 보이고, 이어서 대화·작업 가능
- 클라우드 세션에서 만든 파일은 커밋·푸시되면 GitHub에 즉시 반영되고, 맥북 로컬 폴더에는 다음 세션을 열 때 `git pull`로 내려옵니다.

## 3. 휴대폰 · 아이패드에서 보는 방법

### 작업 공간 (이 저장소)
- **Claude 앱** (iPhone/iPad): 맥북에서 하던 Claude Code 세션을 그대로 이어서 작업할 수 있습니다.
  GitHub 저장소(`linzy-SNU/Claude`)가 연결되어 있어 어느 기기에서든 같은 내용을 봅니다.
- **GitHub 앱 또는 github.com**: 파일을 바로 열람할 수 있습니다.

### 산출물 (에세이, 발표자료 등)
- iCloud Drive `04.Claude_Output/` 폴더는 애플 계정으로 자동 동기화됩니다.
- 아이폰/아이패드의 **파일 앱 → iCloud Drive → 04.Claude_Output**에서 바로 열립니다.
- 따로 설정할 것 없이, 맥북에서 저장되면 몇 초~몇 분 안에 나타납니다.

## 4. 자주 묻는 질문

**Q. 휴대폰에서 수정한 내용은 맥북에 반영되나요?**
네. Claude 앱이나 GitHub에서 수정한 내용은, 맥북에서 다음 세션을 열 때 자동 `git pull`로 내려옵니다.

**Q. 세션을 안 열고 바로 최신 상태로 맞추려면?**
터미널에서 직접 받아오면 됩니다.

```bash
cd /Users/linzy/Claude && git pull origin main
```

**Q. 자동 커밋이 안 된 것 같으면?**
로그를 확인하세요. 건너뛴 이유(자격증명 의심 파일 등)가 기록됩니다.

```bash
tail -20 ~/Library/Logs/claude-git-hook.log
```
