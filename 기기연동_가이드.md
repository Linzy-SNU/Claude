# 맥북 ↔ 휴대폰 · 아이패드 실시간 연동 가이드

맥북의 Claude 작업 공간을 휴대폰과 아이패드에서 실시간으로 보고 이어서 작업하는 방법입니다.
연동은 두 가지 통로로 이루어집니다.

```
맥북 /Users/linzy/Claude/  ──(자동 푸시, 5분마다)──▶  GitHub  ──▶  휴대폰/아이패드 (Claude 앱, GitHub 앱)
맥북 04.Claude_Output/     ──(iCloud 자동 동기화)──▶  iCloud  ──▶  휴대폰/아이패드 (파일 앱)
```

## 1. 맥북에서 설치 (한 번만)

터미널을 열고 아래 명령을 실행하세요.

```bash
cd /Users/linzy/Claude && git pull origin main && zsh scripts/install-autosync.sh
```

이후부터는 맥북이 켜져 있는 동안 **5분마다 자동으로** 작업 공간이 GitHub에 커밋·푸시됩니다.
별도로 할 일은 없습니다.

- 동기화 로그 확인: `~/Library/Logs/claude-autosync.log`
- 주기 변경: `scripts/com.linzy.claude-autosync.plist`의 `StartInterval` 숫자 수정 (초 단위) 후 설치 스크립트 재실행
- 중지: `launchctl bootout gui/$(id -u) ~/Library/LaunchAgents/com.linzy.claude-autosync.plist`

## 2. 새 세션은 항상 클라우드 방식으로

Claude Code 세션을 **클라우드(웹) 방식**으로 열면 작업이 GitHub 저장소 위에서 바로 이루어지므로,
맥북·아이패드·휴대폰 어디서든 같은 세션을 실시간으로 이어서 쓸 수 있습니다.

- **맥북**: claude.ai/code 또는 Claude 앱에서 `linzy-SNU/Claude` 저장소를 선택해 새 세션 시작
- **아이패드/휴대폰**: Claude 앱을 열면 진행 중인 클라우드 세션이 그대로 보이고, 이어서 대화·작업 가능
- 클라우드 세션에서 만든 파일은 커밋·푸시되면 GitHub에 즉시 반영되고, 맥북 로컬 폴더에는 자동 동기화(아래 1번 설치 기준 5분 이내)로 내려옵니다.

## 3. 휴대폰 · 아이패드에서 보는 방법

### 작업 공간 (이 저장소)
- **Claude 앱** (iPhone/iPad): 맥북에서 하던 Claude Code 세션을 그대로 이어서 작업할 수 있습니다.
  GitHub 저장소(`linzy-SNU/Claude`)가 연결되어 있어 어느 기기에서든 같은 내용을 봅니다.
- **GitHub 앱 또는 github.com**: 파일을 바로 열람할 수 있습니다.

### 산출물 (에세이, 발표자료 등)
- iCloud Drive `04.Claude_Output/` 폴더는 애플 계정으로 자동 동기화됩니다.
- 아이폰/아이패드의 **파일 앱 → iCloud Drive → 04.Claude_Output**에서 바로 열립니다.
- 따로 설정할 것 없이, 맥북에서 저장되면 몇 초~몇 분 안에 나타납니다.

## 4. 자주 묻는 질문

**Q. 맥북이 잠자기 상태면?**
동기화가 멈췄다가 깨어나면 다시 시작됩니다. 마지막 변경사항은 깨어난 뒤 5분 안에 푸시됩니다.

**Q. 휴대폰에서 수정한 내용은 맥북에 반영되나요?**
네. Claude 앱이나 GitHub에서 수정하면, 맥북의 자동 동기화가 5분마다 `git pull`로 가져옵니다.

**Q. 더 빠른 실시간이 필요하면?**
`StartInterval`을 `60`(1분)으로 줄이면 됩니다. 1번 항목의 주기 변경 방법을 참고하세요.
